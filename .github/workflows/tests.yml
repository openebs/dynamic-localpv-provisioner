name: Tests

on:
  workflow_call:
    inputs:
      coverage:
        description: Upload code coverage
        required: false
        default: true
        type: boolean

jobs:
  unit-tests:
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Nix
        uses: ./.github/actions/setup-nix

      - name: Unit test
        run: |
          nix-shell --run "make test"
          nix-shell --run "nixfmt **/*.nix"

      - name: Verify corrections
        run: make verify-src

      # todo: this coverage was previously not reported, and since the value is rather low
      # it's failing the checks
      # Also the bdd coverage is incorrect, it's not collecting the data from the pods...
      # P. S.: Above comes directly out of openebs/zfs-localpv. I lack context here, won't worry
      # about this for now.
      - uses: codecov/codecov-action@v7
        if: inputs.coverage
        with:
          use_oidc: true
          directory: coverage
          flags: unittests
          fail_ci_if_error: true
          dry_run: true

  integration-test:
    runs-on: ubuntu-22.04
    needs: ["unit-tests"]
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: true
      matrix:
        kubernetes: [v1.34.3]
    steps:
      - uses: actions/checkout@v7

      - uses: ./.github/actions/setup-go

      - name: Install test tooling
        run: make bootstrap

      - name: Build images locally
        run: make provisioner-localpv-image

      - uses: ./.github/actions/setup-minikube
        with:
          kubernetes-version: ${{ matrix.kubernetes }}

      - name: Set up infra for integration test
        run: sudo apt-get update && sudo apt-get install -y xfsprogs quota linux-modules-extra-`uname -r`

      # Runs first, on a cluster with nothing installed on it yet, which is what
      # the upgrade tests need. They uninstall the release on their way out.
      - name: Upgrade test
        run: make upgrade-test

      - name: Integration test
        run: |
          make helm-install
          make integration-test

      - uses: codecov/codecov-action@v7
        if: inputs.coverage
        with:
          use_oidc: true
          disable_search: true
          files: ./tests/integration_coverage.txt
          flags: integrationtests
          fail_ci_if_error: true

  results:
    runs-on: ubuntu-22.04
    needs: ['integration-test']
    if: ${{ !cancelled() }}
    steps:
      - if: ${{ needs.integration-test.result != 'success' }}
        run: exit 1
      - run: exit 0
